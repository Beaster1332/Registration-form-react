import React, { Component } from "react";

class Submit extends Component {
    render () {
        return (
            <button className="authBtn">Вход</button>
        )
    }
}

export default Submit;